service: hello-serverless-training

frameworkVersion: ">=1.2.0 <2.0.0"

provider:
  name: aws
  runtime: python2.7

functions:
  hello:
          handler: handler.endpoint
          events:
          - http:
          path: hello
          method: get
